Stop or shrink the project if any of these hold:

- The measured gains come mostly from ordinary retrieval or ordinary project instructions, not governed promotion. **`inferred`**
- Accepted artifacts are mostly generic memories or generic skills that could have been static docs. **`inferred`**
- Reviewer burden dominates value. **`inferred`**
- Artifact activation causes frequent regressions and blame is too weak to localize them. **`inferred`**
- The first host cannot expose stable evidence refs from runs. **`inferred`**
- The host/runtime integration starts to require runtime replacement rather than wrapping. **`inferred`**

If those happen, the correct decision is **`defer thesis`**.

